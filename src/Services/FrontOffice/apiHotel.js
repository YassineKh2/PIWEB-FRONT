// services/hotelService.js
/*
import axios from 'axios';

const apiURL = "http://localhost:3000/listhotels";

const hotelService = {
  getHotels: async (cityCode) => {
    try {
      const response = await axios.get(`${apiURL}/getHotels?cityCode=${cityCode}`);
      return response.data;
    } catch (error) {
      console.error('Error fetching hotels:', error.message);
      throw error;  // Propagate the error back to the component
    }
  },
};

export default hotelService;
*/